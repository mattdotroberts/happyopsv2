---
import BaseLayout from '../layouts/BaseLayout.astro';
import CTASection from '../components/CTASection.astro';
import Testimonial from '../components/Testimonial.astro';

const results = [
  { metric: "250+", label: "Hours saved annually" },
  { metric: "11", label: "Hours of training" },
  { metric: "6", label: "Systems automated" },
];

const outcomes = [
  "Email Classification System",
  "Automated Client Feature Requests",
  "Internal Knowledge Bot",
  "Increased Strategic Focus",
  "Data Integration",
  "Weekly Management Meetings powered by Slack",
];

const testimonials = [
  {
    quote: "Why I would suggest other companies work with Happy Operators: definitely to get started because it takes hours to collect all of that information yourself, find a video that kind of guides you through the process and it takes you probably a week to just get started and to have something successfully running.",
    author: "Isabel Grun",
    role: "Senior Product Manager",
    image: "/images/case-study/isabel-grun.jpg",
  },
  {
    quote: "I can see the impact that it had on the team from people who really didn't know about n8n and I can see the adoption here. Not everyone will pick up and I think it's normal with any discipline, right? But that will definitely have, again, a compounding effect on the value that we can generate for the company by automating things.",
    author: "Perlin Gentil",
    role: "Director Strategic Accounts",
    image: "/images/case-study/perlin-gentil.jpg",
  },
];
---

<BaseLayout
  title="Case Study: JamesEdition"
  description="How JamesEdition saved 250+ hours annually with AI automation training. A real-world example of transforming team productivity through workshops."
>
  <div class="container">
    <header class="page-header">
      <span class="page-header__label">[CASE STUDY]</span>
      <img src="/images/case-study/jamesedition-logo.png" alt="JamesEdition" class="page-header__logo" />
      <h1 class="page-header__title">JamesEdition</h1>
      <p class="page-header__subtitle">
        Luxury Real Estate Industry
      </p>
    </header>

    <hr />

    <section class="results-bar">
      {results.map((result) => (
        <div class="result-item">
          <span class="result-item__metric">{result.metric}</span>
          <span class="result-item__label">{result.label}</span>
        </div>
      ))}
    </section>

    <section class="content-section">
      <h2 class="content-section__title">The Problem</h2>

      <div class="problem-grid">
        <div class="problem-card">
          <h3 class="problem-card__title">Process-Heavy Operations</h3>
          <p class="problem-card__description">
            Sales and marketing teams were drowning in repetitive tasks - weekly reporting that consumed hours, manual client follow-ups, and data synchronization across multiple systems.
          </p>
        </div>

        <div class="problem-card">
          <h3 class="problem-card__title">Technical Dependency</h3>
          <p class="problem-card__description">
            Every automation request required developer resources, creating delays and limiting the team's ability to solve their own problems quickly.
          </p>
        </div>

        <div class="problem-card">
          <h3 class="problem-card__title">Knowledge Silos</h3>
          <p class="problem-card__description">
            Process owners understood their workflows best but lacked the technical tools to automate them, while developers had the technical skills but limited domain knowledge.
          </p>
        </div>
      </div>
    </section>

    <section class="quote-section">
      <blockquote class="featured-quote">
        <p class="featured-quote__text">"Those who run it, fix it best."</p>
        <footer class="featured-quote__footer">
          <img src="/images/case-study/eric-finnas.png" alt="Eric Finnas" class="featured-quote__image" />
          <div class="featured-quote__info">
            <cite class="featured-quote__author">Eric Finnas</cite>
            <span class="featured-quote__role">CEO, JamesEdition</span>
          </div>
        </footer>
      </blockquote>

      <div class="featured-quote__context">
        <p>
          "For us, doing a workshop was very efficient and effective, both in terms of having a reason to get the whole management team together, to sit together and solve problems, but also coming prepared with specific use cases.
        </p>
        <p>
          We could already, during a few days together, actually solve a lot of processes already there together. So both being able to work together physically and learning from each other, but also learning from Happy Operators being in that room and iterating really fast was a very good method for us, at least, to accelerate the learning."
        </p>
      </div>
    </section>

    <section class="content-section">
      <h2 class="content-section__title">What We Did</h2>
      <p class="content-section__intro">
        We proposed a mix of workshop and troubleshooting sessions:
      </p>

      <div class="approach-grid">
        <div class="approach-card">
          <span class="approach-card__hours">4 Hours</span>
          <h3 class="approach-card__title">Workshop</h3>
          <p class="approach-card__description">
            Covering how n8n works, how to build Agents, how to handle errors, how to use MCPs, etc.
          </p>
        </div>

        <div class="approach-card">
          <span class="approach-card__hours">7 Hours</span>
          <h3 class="approach-card__title">Troubleshooting</h3>
          <p class="approach-card__description">
            They proposed a list of bottlenecks and systems to automate, we carefully studied them and helped implement on the go during 2 sessions with them.
          </p>
        </div>
      </div>
    </section>

    <section class="content-section">
      <h2 class="content-section__title">What They Got</h2>
      <p class="content-section__intro">
        They ended up with knowledge about n8n and an array of new systems ready to implement:
      </p>

      <div class="outcomes-grid">
        {outcomes.map((outcome) => (
          <div class="outcome-item">
            <span class="outcome-item__icon">✓</span>
            <span class="outcome-item__text">{outcome}</span>
          </div>
        ))}
      </div>
    </section>

    <section class="content-section">
      <h2 class="content-section__title">What The Team Says</h2>

      <div class="testimonials-grid">
        {testimonials.map((t) => (
          <Testimonial quote={t.quote} author={t.author} role={t.role} image={t.image} />
        ))}
      </div>
    </section>
  </div>

  <CTASection
    headline="Ready to transform your team's productivity?"
    description="Book a free discovery call to discuss how we can help your team save hours every week."
    ctaText="Schedule a Call"
    ctaHref="/contact"
    variant="centered"
  />
</BaseLayout>

<style>
  .page-header {
    padding: var(--space-2xl) 0;
    text-align: center;
  }

  .page-header__label {
    display: block;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--color-accent-primary);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
    margin-bottom: var(--space-md);
  }

  .page-header__title {
    font-size: var(--text-4xl);
    margin-bottom: var(--space-sm);
  }

  .page-header__logo {
    max-width: 200px;
    height: auto;
    margin-bottom: var(--space-lg);
  }

  .page-header__subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
  }

  .results-bar {
    display: flex;
    justify-content: center;
    gap: var(--space-4xl);
    padding: var(--space-3xl) 0;
    border-bottom: 1px solid var(--color-border);
    margin-bottom: var(--space-3xl);
  }

  .result-item {
    text-align: center;
  }

  .result-item__metric {
    display: block;
    font-size: var(--text-4xl);
    font-weight: 700;
    color: var(--color-accent-primary);
    line-height: 1;
    margin-bottom: var(--space-xs);
  }

  .result-item__label {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  .content-section {
    padding: var(--space-3xl) 0;
  }

  .content-section__title {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-lg);
  }

  .content-section__intro {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-xl);
  }

  .problem-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
  }

  .problem-card {
    padding: var(--space-xl);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius-md);
  }

  .problem-card__title {
    font-size: var(--text-lg);
    margin-bottom: var(--space-sm);
    color: var(--color-accent-primary);
  }

  .problem-card__description {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .quote-section {
    padding: var(--space-4xl) 0;
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
    margin: var(--space-3xl) 0;
  }

  .featured-quote {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }

  .featured-quote__text {
    font-size: var(--text-3xl);
    font-family: var(--font-serif);
    font-style: italic;
    margin-bottom: var(--space-lg);
  }

  .featured-quote__footer {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-md);
  }

  .featured-quote__image {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    object-fit: cover;
  }

  .featured-quote__info {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    text-align: left;
  }

  .featured-quote__author {
    font-size: var(--text-lg);
    font-weight: 600;
    font-style: normal;
  }

  .featured-quote__role {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .featured-quote__context {
    max-width: var(--max-width-prose);
    margin: 0 auto;
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  .featured-quote__context p {
    margin-bottom: var(--space-md);
  }

  .approach-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
  }

  .approach-card {
    padding: var(--space-xl);
    background: var(--color-bg-secondary);
    border-radius: var(--border-radius-md);
  }

  .approach-card__hours {
    display: inline-block;
    padding: var(--space-xs) var(--space-sm);
    background: var(--color-accent-primary);
    color: var(--color-bg-primary);
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    border-radius: var(--border-radius-sm);
    margin-bottom: var(--space-md);
  }

  .approach-card__title {
    font-size: var(--text-xl);
    margin-bottom: var(--space-sm);
  }

  .approach-card__description {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .outcomes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-md);
  }

  .outcome-item {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-lg);
    background: var(--color-bg-secondary);
    border-radius: var(--border-radius-sm);
  }

  .outcome-item__icon {
    color: var(--color-accent-primary);
    font-weight: 700;
  }

  .outcome-item__text {
    color: var(--color-text-primary);
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-xl);
  }

  @media (max-width: 640px) {
    .results-bar {
      flex-direction: column;
      gap: var(--space-xl);
    }
  }
</style>
